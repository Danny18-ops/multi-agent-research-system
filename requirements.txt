# ── Core LLM & Agent Frameworks ───────────────────────────────────────────────
crewai>=0.86.0,<1.0.0       # latest release supporting Python <=3.13
crewai-tools>=0.14.0,<1.0.0
langgraph>=0.2.0,<2.0.0
langgraph-checkpoint-sqlite>=3.0.0  # SQLite state persistence for LangGraph
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-community>=0.3.0
openai>=1.40.0

# ── Web Search & Scraping ──────────────────────────────────────────────────────
tavily-python>=0.3.0          # Tavily AI search (recommended)
duckduckgo-search>=6.0.0      # Free fallback search (no API key)
requests>=2.32.0
beautifulsoup4>=4.12.0
lxml>=5.0.0                   # Fast HTML/XML parser for bs4

# ── Configuration & Validation ────────────────────────────────────────────────
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.0

# ── Utilities ─────────────────────────────────────────────────────────────────
tenacity>=8.3.0               # Retry logic with exponential back-off
rich>=13.7.0                  # Pretty console output / progress bars
tiktoken>=0.7.0               # Token counting for OpenAI models

# ── Development & Testing ─────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-mock>=3.14.0
black>=24.0.0
ruff>=0.4.0
mypy>=1.10.0
